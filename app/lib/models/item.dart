class Item {
  final int id;
  final String title;
  final String? content;
  final DateTime createTime;
  final DateTime modifyTime;

  Item(this.id, this.title, this.content, this.createTime, this.modifyTime);
}
